name: PR Version Labeler

on:
  pull_request:
    types: [opened, edited]
        
jobs:
  version-labeler:
    uses: alphaprime-dev/alpha-actions/.github/workflows/pr-version-labeler.yml@workflow
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}