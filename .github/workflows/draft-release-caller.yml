name: draft-release

on:
  pull_request:
    types: [closed]

jobs:
  draft-release:
    uses: alphaprime-dev/alpha-actions/.github/workflows/draft-release.yaml@refactoring-config
    secrets: inherit
