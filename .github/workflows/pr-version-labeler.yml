name: PR Version Labeler

on:
  pull_request:
    types: [opened, edited]
  
  push:
    branch:
      - test-config


jobs:
  version-labeler:
    uses: alphaprime-dev/alpha-actions/.github/workflows/pr-version-labeler.yaml@refactoring-config
    secrets: inherit