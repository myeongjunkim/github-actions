name: PR Version Labeler

on:
  pull_request:
    types: [opened, edited]
  
  push:
    branches:
      - test-config


jobs:
  version-labeler:
    uses: alphaprime-dev/alpha-actions/.github/workflows/pr-version-labeler.yaml@feat/notify-discord
    secrets: inherit